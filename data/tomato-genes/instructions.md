# Instructions — tomato-genes

Ingest **only** `Additional file 1.xlsx`.

Ignore the PDF files (`additional file 2.pdf`, `additional file 3.pdf`). Do not extract
text from them and do not create a `documents` table for them. List them as
deliberately skipped in `semantics.md`.

All four sheets of the workbook (`Table S1` through `Table S4`) are in scope.
