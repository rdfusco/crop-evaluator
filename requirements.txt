# Dashboard runtime dependencies.
#   pip install -r requirements.txt
#
# Verified working on Python 3.12.10 with:
#   fastapi 0.128.0, uvicorn 0.40.0, pydantic 2.11.4, numpy 1.26.4, scipy 1.15.2

fastapi>=0.110
uvicorn>=0.30
pydantic>=2.0
numpy>=1.26
scipy>=1.11

# Ingesting a new dataset (see ingest.md) additionally needs a spreadsheet reader,
# and a PDF reader if the sources include PDFs. Not required to run the dashboard:
#
#   pip install pandas openpyxl pypdf
